

####动态管理定时任务（quartz包）####
目的：动态管理定时任务，如单个固定的定时任务可通过Spring的@Scheduled注解配置。

特性：1>可配置、通用性；2>动态管理；3>大批量操作。

原理及组成：
	1>任务列表，保存在数据库(包含任务key、任务定时配置、任务对应执行类等)；
	2>任务管理类（需包含Scheduler类实例，控制任务）；
	3>任务处理类（Job类，解析任务将任务发发到指定执行类）；
	4>任务执行类（自定义，与具体任务对应）。






